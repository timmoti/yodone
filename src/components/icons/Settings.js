import React from 'react';
import Icon from '../../Icon';
import { ICONS } from '../../constants';

const Settings = () => {
  return (
    <Icon icon={ICONS.SETTINGS} size={16} color={'rgba(255, 255, 255, 0.32)'} />
  );
};

export default Settings;
